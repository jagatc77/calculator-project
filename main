#include <iostream>
using namespace std;
void add(int b,int c){
    cout<<b+c<<endl;
    
} 
int sub(int f,int g){
        return(f-g);
}

float divide(float  a,float  b){
    return(a/b);
    
}
int multiply(int a,int b){
     return(a*b);
}
    
float percentage(float c,float d){
    return((c/d)*100);
}
float square(float c){
    return(c*c);
}
int main() {
   char ch;
   cout<<"enter the operation : ";
   cin>>ch;
   
    
     if(ch == '+'){
     float a,b;
    cout<<"enter the value of a : ";
    cin>>a;
    cout<<"enter the value of b : ";
    cin>>b;
    add(a,b);
    }
    else if(ch == '-'){
     float a,b;
    cout<<"enter the value of a : ";
    cin>>a;
    cout<<"enter the value of b : ";
    cin>>b;
    sub(a,b);
    }
    else if(ch == '*'){
         int c,d;
    cout<<"enter the value of c : ";
    cin>>c;
    cout<<"enter the value of d : ";
    cin>>d;
    cout<<multiply(c,d)<<endl;
    }
    else if (ch == '/'){
         int c,d;
    cout<<"enter the value of c : ";
    cin>>c;
    cout<<"enter the value of d : ";
    cin>>d;
     cout<<divide(c,d)<<endl;
        
    }
    else if(ch == '%'){
          float c,d;
    cout<<"enter the value of c : ";
    cin>>c;
    cout<<"enter the value of d : ";
    cin>>d;
    cout<<percentage(c,d)<<endl;
    }
    else if(ch == '^'){
        float c,d;
    cout<<"enter the value of c : ";
    cin>>c;
    cout<<square(c)<<endl;
    }
    return 0;
}
